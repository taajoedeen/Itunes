{
  "services": [
    {
      "type": "web",
      "name": "server",
      "buildCommand": ["npm install", "npm run build"],
      "startCommand": "npm start",
      "root": "server"
    },
    {
      "type": "web",
      "name": "frontend",
      "buildCommand": ["npm install", "npm run build"],
      "startCommand": "npm start",
      "root": "client"
    }
  ]
}
