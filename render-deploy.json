{
  "services": [
    {
      "type": "web",
      "name": "server",
      "root": "server",
      "buildCommand": ["npm install", "npm run build"],
      "startCommand": "npm start"
    },
    {
      "type": "web",
      "name": "frontend",
      "root": "client",
      "buildCommand": ["npm install", "npm run build"],
      "startCommand": "npm start"
    }
  ]
}
